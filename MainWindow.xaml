<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sample">

    <Grid x:Name="Grid1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{x:Bind VV1,Mode=OneWay}" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Demonstration of Resizer -->
        <Rectangle Fill="LightCyan" Grid.Row="0" Grid.Column="0"></Rectangle>
        <local:Resizer Grid.Row="0" Grid.Column="1" Value="{x:Bind V1,Mode=TwoWay}" Orientation="Vertical" Minimum="-200" Maximum="200" HorizontalAlignment="Stretch" />
        <Rectangle Fill="Red" Grid.Row="0" Grid.Column="2"></Rectangle>
        
        
        <!-- Demonstration of StaggeredLayout -->
        <ScrollViewer Grid.Row="1" Grid.Column="0">
            <ItemsRepeater VerticalAlignment="Stretch" x:Name="MyRepeater"  ItemsSource="{x:Bind TheItems,Mode=OneWay}">
                <ItemsRepeater.Layout>
                    <local:StaggeredLayout ColumnCount="10"/>
                </ItemsRepeater.Layout>

                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <Rectangle Fill="SteelBlue"
                               Width="50"
                                   HorizontalAlignment="Stretch"
                               Height="{x:Bind Number1}"  Margin="1"/>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

<!--        <local:RangeSelect Grid.Row="1" Grid.Column="1" 
    Width="300"
    Height="40"
    Minimum="0"
    Maximum="100"
    StartValue="20"
    EndValue="80"
                           Orientation="Horizontal"/>
-->

    </Grid>




</Window>
