<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sample">

    <Style TargetType="local:RangeSelect">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RangeSelect">
                    <Grid>
                        <Rectangle x:Name="Track" Height="4" VerticalAlignment="Center" Fill="LightGray" />
                        <Rectangle x:Name="RangeHighlight" Height="4" VerticalAlignment="Center" Fill="DodgerBlue" />
                        <Thumb x:Name="StartThumb" Width="12" Height="12" Background="White" BorderBrush="Black" BorderThickness="1"/>
                        <Thumb x:Name="EndThumb" Width="12" Height="12" Background="White" BorderBrush="Black" BorderThickness="1"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:Resizer">
		<Setter Property="Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}" />
		<Setter Property="BorderBrush" Value="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Padding" Value="2" />
		<Setter Property="MinWidth" Value="2" />
		<Setter Property="MinHeight" Value="2" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Stretch" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="local:Resizer">
					<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            IsHitTestVisible="True"
                            >
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>